[url_schema]
name = VAR(255) NOT NULL UNIQUE

[url_domain]
name = VAR(255) NOT NULL UNIQUE

[url_title]
name = VAR(255) NOT NULL UNIQUE

[url]
schema_id = INTEGER NOT NULL REF(url_schema)
domain_id = INTEGER NOT NULL  REF(url_domain)
path = VAR(255) NOT NULL
title_id = INTEGER NOT NULL REF(url_title)


[human_fname]
name = VAR(255) NOT NULL

[human_sname]
name = VAR(255) NOT NULL

[human_tname]
name = VAR(255)

[human]
fname = INTEGER NOT NULL REF(human_fname)
sname = INTEGER NOT NULL REF(human_sname)
tname = INTEGER REF(human_tname)
date_birth = DATETIME
date_dearh = DATETIME
